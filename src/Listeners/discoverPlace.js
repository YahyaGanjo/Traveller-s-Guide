import discoverPlaceHandler from "../Handlers/discoverPlaceHandler.js";

function discoverPlace(page, discover, place) {
  discoverPlaceHandler(page, discover, place);
}
export default discoverPlace;
